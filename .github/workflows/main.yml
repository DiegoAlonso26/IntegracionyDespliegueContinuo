name: Docker Deploy Local

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: self-hosted

    steps:
    - name: Checkout código
      uses: actions/checkout@v3

    - name: Desplegar contenedor con Docker
      run: |
        docker compose down
        docker compose up -d --build
